---
title: 자립명사 결합 (jarip_noun)
description: 율률, 열렬 같은 자립명사형 결합 처리
---

특정 명사형 교체 규칙이 필요한 경우에 사용합니다.

```python
jarip_noun(word: str, particle: str) -> str
```

## 예시

```python
from hangulpy import jarip_noun

print(jarip_noun("확", "율/률"))  # 확률
print(jarip_noun("직", "열/렬"))  # 직렬
print(jarip_noun("명", "영/령"))  # 명령
print(jarip_noun("신", "염/념"))  # 신념
print(jarip_noun("범", "예/례"))  # 범례
```

## 같이 보기

- [josa](josa)

## 적용 범위

`율/률`, `열/렬`은 모음 또는 ㄴ 받침 뒤에서 `율`, `열`을 선택합니다.
`영/령`, `염/념`, `예/례`는 단어 내부의 본음 `령`, `념`, `례`를 붙입니다
(요령, 기념, 사례). 이 함수는 지정한 한자음의 결합을 돕고, 서로 다른 한자의
뜻이나 임의의 합성어 경계를 추론하지 않습니다.
